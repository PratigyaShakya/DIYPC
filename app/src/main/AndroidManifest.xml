<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:label="Register"/>

        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"
            android:label="Login"
       />
        <activity android:name=".BuilderActivity"
            android:label="Pick CPU"/>

        <activity android:name=".ListPartsBuilder"
            android:label="Parts Collection"/>

        <activity android:name=".CPUDetailActivity"
            android:label="CPU Detail"/>

        <activity android:name=".BufferActivity"
            android:label="Builds"/>

        <activity android:name=".MotherboardActivity"
            android:label="Pick Motherboard"/>

        <activity android:name=".MotherboardDetailedActivity"/>
        <activity android:name=".CaseActivity"/>

    </application>
</manifest>